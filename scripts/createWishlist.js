import { auth } from './index.js';

export const createWishlist = login => {
  const login = auth.login;

  if (pageLogin) {
    pageLogin = login;
  }

  // const user = await getUser(pageLogin);
  // console.log('user: ', user);
  
}